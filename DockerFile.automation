# Usa la imagen base de Cypress
FROM cypress/included:latest

# Configura el directorio de trabajo
WORKDIR /e2e

# Copia el proyecto al contenedor
COPY . .

# Comando por defecto (puedes sobreescribir con "entrypoint" o "command" en docker-compose)
ENTRYPOINT ["npx", "cypress", "run"]
